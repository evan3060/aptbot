# aptbot 0.3.0 UAT 核验清单

> **状态：⏳ 待用户验收**（封仓收尾，文档 + UAT 清单就位，等待用户核验）
>
> **前置条件：** 0.3.0 实施任务（Task 1-20）已完成，全量测试套件 0 退化（除 ~2 pre-existing auth-api ECONNRESET flaky 已知时序问题），`npx tsc --noEmit` 0 错误
>
> **核验方式：** 用户逐项核验，通过的勾选 `[x]`，不通过的标记 `❌` 并记录问题，全部修复后重新 UAT
>
> **当前分支：** `feat/0.3.0`
> **基线 commit：** `6a734f7` + Task 17/18/19/20 commits
>
> **核验配置：** 启用 `landingPage: true` + `learnPage: true` + `feedbackEnabled: true`（沿用 0.2.3 配置），无需新增 config 字段（agent 系统默认启用）
>
> **代验说明：**
> - Web UI 项由用户浏览器确认
> - CLI 项（/agent + /skill）由 assistant 通过 CommandRegistry 真实路径探针代验
> - 测试套件覆盖项在核验时附注测试文件
> - VPS 项待部署后核验

---

## 范围 1：本地功能验证

### 1.1 启动与配置

- [ ] `npm run dev` 启动本地服务器无报错（启动时 `migrateLegacySessions` 执行，无 warning）
- [ ] 启动后 `data/users/<userId>/agents/default/` 目录自动创建（含 AGENT.md）
- [ ] legacy `data/sessions/*.jsonl` 已迁移到 `data/users/<userId>/agents/default/sessions/`（按 userId 分组）
- [ ] 无 legacy session 时启动正常（空迁移报告 migratedSessions=0）
- [ ] config 不含 0.3.0 新字段时启动正常（agent 系统默认启用，无 opt-in 字段）

### 1.2 访问 WebUI

- [ ] 浏览器打开 `http://localhost:8080/` 加载聊天页面正常
- [ ] 左侧栏显示 agent 树形结构（default agent 根节点 + 已有 session 叶节点）
- [ ] 左侧栏「+ 新建专业 agent」按钮可见
- [ ] 当前 agent + 当前 session 高亮显示
- [ ] agent 节点默认展开（显示子会话）
- [ ] 点击 agent 名折叠/展开子会话列表

### 1.3 agent sidebar 操作

- [ ] 点击「+ 新建会话」按钮创建新 session（归属当前 agent）
- [ ] 点击 session 切换会话（agentId 归属显示正确）
- [ ] 点击 agent 节点 ⚙️ 设置按钮打开设置浮层
- [ ] default agent 不显示删除按钮（仅专业 agent 显示）
- [ ] 「+ 新建专业 agent」按钮打开新建 agent 浮层

### 1.4 skill chip 区（仅 default agent）

- [ ] default agent 会话上方显示 skill chip 区（visibleSkills 非空时）
- [ ] 点击 chip 填模板到输入框（`{{cursor}}` 占位符光标定位正确）
- [ ] 空输入框 → 覆盖填入；有内容 → 追加
- [ ] 选中 chip 高亮（可再点取消）
- [ ] 切换到专业 agent 会话时 chip 区不渲染
- [ ] visibleSkills 为空时 chip 区不渲染

### 1.5 记忆写入 toast（仅 professional agent）

- [ ] professional agent 会话中 agent 调用 write_agent_memory 工具时显示 toast
- [ ] toast 显示 section 名 + 简短预览（如「agent 已更新记忆：Preferences」）
- [ ] toast 自动消失（3-5 秒后）
- [ ] toast 不打断对话（位置在右上角或底部）

### 1.6 CLI /agent + /skill 命令

- [ ] `/agent` 列出所有 agents（标记当前）
- [ ] `/agent <slug>` 切换到指定 agent
- [ ] `/agent info` 显示当前 agent 的 AGENT.md 内容
- [ ] `/agent memory-log` 列出最近 20 条记忆写入日志
- [ ] `/agent memory-log 5` 列出最近 5 条
- [ ] 未知 `/agent <slug>` 返回错误 + 列出可用 agents
- [ ] `/skill` 列出所有已加载 skill
- [ ] `/skill use <name>` 激活 skill 填模板
- [ ] `/help` 列出 `/agent` + `/skill` 命令

---

## 范围 2：VPS 部署核验（如适用）

> **注意：** 0.3.0 部署到 aptbot.de 后核验。沿用 0.2.3 配置（landingPage + learnPage + feedbackEnabled 均已启用）。

- [ ] 部署到 VPS 后服务正常启动（aptbot.de 主站 HTTP 200）
- [ ] HTTPS 证书有效，无混合内容警告
- [ ] agent API 路由可达：`GET /api/agents`（需 Bearer token，无 token 返回 401）
- [ ] skill API 路由可达：`GET /api/skills`（需 Bearer token）
- [ ] WebSocket 连接正常（chat 页面消息发送 + 流式回复正常）
- [ ] legacy session 自动迁移到新路径（`data/users/<userId>/agents/default/sessions/`）
- [ ] HTTP 响应头含 `Cache-Control: no-store` + `X-Content-Type-Options: nosniff` + HSTS + X-Frame-Options: DENY + Referrer-Policy（沿用 0.2.3）

---

## 范围 3：新功能逐项核验

### 3.1 双轨 agent 系统（Task 1 + Task 9 + Task 12）

- [ ] AgentProfile 类型支持 `default` 与 `professional` 两种 type
- [ ] GET `/api/agents` 列出当前用户的所有 agents
- [ ] POST `/api/agents` 创建新 professional agent（slug 自动生成 `agent-<6-hex-chars>`）
- [ ] POST `/api/agents` 超 50 个 → 400（MAX_AGENTS_PER_USER）
- [ ] PUT `/api/agents/:slug` 更新 agent 配置（personality / LLM / memoryEnabled）
- [ ] GET `/api/agents/:slug` 获取 agent 详情
- [ ] 跨用户访问 → 403（agent.userId !== currentUserId）
- [ ] 不存在的 slug → 404
- [ ] 非法 slug（路径遍历） → 400
- [ ] `/agent` CLI 命令注册在 CommandRegistry
- [ ] `/agent` 命令在 agent 之前拦截（不下发给 agent）

### 3.2 AgentProfile 类型（Task 1 + Task 18）

- [ ] `AgentProfile` 接口含全部字段（name / description / userId / type / slug / createdAt / updatedAt / personality）
- [ ] LLM 配置字段全部可选（model / temperature / maxTokens / reasoningEffort / thinkingType / thinkingBudgetTokens）
- [ ] `memoryEnabled` 字段（Task 18，缺省视为 true；仅 professional 生效；default 永不注入）
- [ ] `AGENT_SLUG_REGEX = /^[a-z0-9-]{3,64}$/` 路径遍历防护
- [ ] `MAX_MEMORY_SIZE = 8192`、`MAX_WRITE_CONTENT_SIZE = 2048`、`MAX_AGENTS_PER_USER = 50` 常量
- [ ] `AgentProfileSchema` zod 校验通过 / 失败用例
- [ ] `parseAgentMd(raw)` 解析 AGENT.md（gray-matter 复用 0.2.3 依赖）
- [ ] `generateSlug()` 生成 `agent-<6-hex-chars>` 格式
- [ ] `validateSlug(slug)` 正则校验

### 3.3 AgentStorage（Task 2 + Task 19）

- [ ] `getAgent` 读取存在的 agent 返回完整 profile
- [ ] `getAgent` 读取不存在的 agent 返回 null
- [ ] `getAgent` 损坏文件抛错
- [ ] `listAgents` 列出多个 agent；损坏文件 warn 跳过不抛错
- [ ] `saveAgent` 原子写入（write-to-tmp + rename）；自动创建多层目录
- [ ] `saveAgent` 并发写入同一 agent 串行化（per-agentId mutex，5000ms 超时）
- [ ] `deleteAgent` 删除 agent 目录（幂等）
- [ ] `deleteAgent` 在 per-agentId 锁内执行（避免与 saveAgent 竞态）
- [ ] `exists` / `countAgents` 方法正确
- [ ] `findAgentOwner` 跨用户 slug 查找正确（用于 403 检测）
- [ ] `archiveAgent` 复制 → 验证 → 删除原子操作
- [ ] `archiveAgent` 验证失败拒绝删除 + 清理归档目录
- [ ] `archiveAgent` cpSync 失败时清理归档路径防脏数据
- [ ] 归档路径含 AGENT.md + MEMORY.md + sessions/ + memory.log.jsonl
- [ ] 路径遍历防护（非法 userId / slug 抛错）

### 3.4 migration（Task 3）

- [ ] `migrateLegacySessions` 扫描 legacy `data/sessions/*.jsonl` + `.meta.json`
- [ ] 有 userId 的 session 迁移到 `data/users/<userId>/agents/default/sessions/`
- [ ] 无 userId 的 session 生成伪 UUID 作为 userId
- [ ] default agent AGENT.md 自动创建（若不存在）
- [ ] .meta.json 添加 `agentId: default` 字段
- [ ] 重复运行不重复迁移（幂等）
- [ ] 中断后重新运行可继续（atomic write-to-tmp + rename）
- [ ] `MigrationReport` 字段正确（migratedSessions / createdAgents / errors）
- [ ] `ensureDefaultAgent` 用户首次访问时创建 default agent

### 3.5 read_agent_memory 工具（Task 6）

- [ ] 读取存在的 MEMORY.md 返回内容
- [ ] 读取不存在的 MEMORY.md 返回空内容（非错误）
- [ ] 读取 >8KB（MAX_MEMORY_SIZE）文件返回 `memory_too_large` error，提示按 section 分段读取
- [ ] section 过滤正确（user_profile / facts / preferences / history / all，默认 all）
- [ ] 路径硬编码当前 agentId，不接受路径参数
- [ ] 跨 agent 访问禁止（路径硬编码）

### 3.6 write_agent_memory 工具 + 审计日志（Task 7）

- [ ] append 模式追加到 section 末尾
- [ ] replace 模式替换整个 section 内容
- [ ] 单次 content >2KB（MAX_WRITE_CONTENT_SIZE）拒绝写入
- [ ] 路径硬编码当前 agentId
- [ ] write-to-tmp + rename 原子操作
- [ ] 返回 afterSize 给 agent
- [ ] `MemoryAuditLog.append` 记录审计日志（timestamp / sessionId / section / mode / contentPreview / contentLength / beforeSize / afterSize）
- [ ] `MemoryAuditLog.list` 默认返回最近 20 条（最新在前倒序）
- [ ] 审计日志 append-only JSONL，复用 `withJsonlLock` 串行化
- [ ] 审计日志路径 `data/users/<userId>/agents/<slug>/memory.log.jsonl`
- [ ] 审计日志路径遍历防护（userId + slug 校验）
- [ ] 并发写入串行化（per-agentId mutex）

### 3.7 systemPrompt builder（Task 8 + Task 17）

- [ ] `buildSystemPrompt(agent, memoryContent)` 构造完整 systemPrompt
- [ ] default agent systemPrompt 不含 `## Agent Memory` section
- [ ] professional + memoryEnabled=true agent systemPrompt 含 `## Agent Memory` section
- [ ] professional + memoryEnabled=false agent systemPrompt 不含 `## Agent Memory` section
- [ ] MEMORY.md 不存在时注入空 body 的 `## Agent Memory` section
- [ ] personality 非空时注入 `## Personality` section；为空跳过
- [ ] `computeSystemPromptCacheKey` 返回 sha256 hex（64 字符）
- [ ] 相同内容相同 hash（KV 缓存命中）
- [ ] 追加记忆后缓存 key 变化（History section 末尾追加）
- [ ] systemPrompt 安全约束：禁止访问 `data/users/*/agents/*/sessions/`
- [ ] systemPrompt 安全约束：禁止访问 `data/users/*/archived-agents/`
- [ ] systemPrompt 安全约束：禁止访问 ui-config.json
- [ ] systemPrompt 安全约束：允许通过 read_agent_memory / write_agent_memory 工具访问当前 agent 的 MEMORY.md
- [ ] systemPrompt 安全约束：禁止访问其他 agent 的 MEMORY.md
- [ ] server.ts 动态构建 systemPrompt（调用 buildSystemPrompt 替代固定字符串）

### 3.8 agent-api（Task 9 + Task 10 + Task 11 + Task 19）

- [ ] GET `/api/agents` 无 auth → 401
- [ ] GET `/api/agents` 正确 auth → 200 + agents 列表
- [ ] GET `/api/agents/:slug` 获取 agent 详情
- [ ] POST `/api/agents` 创建 professional agent（slug 自动生成）
- [ ] POST `/api/agents` 超 50 个 → 400
- [ ] PUT `/api/agents/:slug` 更新 agent 配置
- [ ] DELETE `/api/agents/default` → 403（不可删）
- [ ] DELETE `/api/agents/:slug`（professional） → 200 + 归档路径（Task 19 接入）
- [ ] GET `/api/agents/:slug/sessions` 列出该 agent 的 sessions
- [ ] GET `/api/agents/:slug/memory` 获取 MEMORY.md 内容
- [ ] GET `/api/agents/:slug/memory-log` 默认 20 条审计日志
- [ ] GET `/api/agents/:slug/memory-log?limit=5` 自定义条数
- [ ] GET `/api/agents/default/ui-config` 返回 UI 配置
- [ ] PUT `/api/agents/default/ui-config` 更新 UI 配置
- [ ] GET `/api/skills` 列出所有已加载 skill（含 template 字段）
- [ ] 路由优先级：`/api/agents` 优先于 `/api/*`
- [ ] 跨用户访问 → 403
- [ ] 路径遍历防护（非法 slug → 400）

### 3.9 ui-config（Task 10）

- [ ] `UiConfigStorage.get` 不存在的 ui-config.json 返回空 visibleSkills
- [ ] `UiConfigStorage.update` 后 `get` 返回相同配置
- [ ] 文件损坏返回空 visibleSkills + warn
- [ ] write-to-tmp + rename 原子写
- [ ] 路径 `data/users/<userId>/agents/default/ui-config.json`（仅 default agent，硬编码 "default"）
- [ ] 路径遍历防护（USER_ID_REGEX）

### 3.10 skill-api（Task 11）

- [ ] GET `/api/skills` 无 auth → 401
- [ ] GET `/api/skills` 正确 auth → 200 + skills 列表
- [ ] 返回字段含 name / description / template（若有）
- [ ] 不返回 skill 文件路径等内部信息

### 3.11 CLI 命令（Task 12）

- [ ] `/agent` 注册在 CommandRegistry
- [ ] `/agent` 列出所有 agents（标记当前）
- [ ] `/agent <slug>` 切换到指定 agent
- [ ] `/agent info` 显示当前 agent 的 AGENT.md 内容
- [ ] `/agent memory-log [limit]` 列出审计日志（默认 20）
- [ ] `/skill` 列出所有已加载 skill
- [ ] `/skill use <name>` 激活 skill 填模板
- [ ] 未知 `/agent <slug>` 返回错误 + 列出可用 agents
- [ ] 命令注册到 CommandRegistry 防止下发给 agent
- [ ] `/help` 列出新增命令

### 3.12 WebUI 8 组件（Task 13-16）

- [ ] `<agent-sidebar>` 渲染 agent 树形结构（容器）
- [ ] `<agent-node>` 渲染 agent 节点（折叠/展开 + ⚙️ 设置按钮）
- [ ] `<session-node>` 沿用 0.2.x + 加 agentId 归属显示
- [ ] `<agent-settings-modal>` 通用模式：LLM 配置区 + Skill 配置区 + 无 personality + 无删除
- [ ] `<agent-settings-modal>` 专业模式：身份 + 性格 + 记忆 + LLM 配置区 + 删除按钮（带确认弹窗）
- [ ] `<new-agent-modal>` 新建专业 agent 浮层（name 输入 + slug 自动生成）
- [ ] `<skill-chips-bar>` chip 区（仅 default agent 显示）
- [ ] `<memory-write-toast>` 记忆写入提示（自动消失 3-5 秒）
- [ ] 居中 modal 560px 宽度 + 半透明黑色背景遮罩（点击不关闭）
- [ ] 底部固定保存 / 取消按钮
- [ ] 删除按钮带确认弹窗
- [ ] 样式沿用 adept tokens CSS 变量 + Inter 字体

### 3.13 server.ts 装配（Task 17）

- [ ] AgentStorage 在启动时实例化（dataDir 注入）
- [ ] MemoryAuditLog 在启动时实例化
- [ ] `migrateLegacySessions` 在启动时调用（一次性迁移）
- [ ] WebSocketServerOptions 注入 agentStorage / memoryAuditLog / handleAgentApi
- [ ] 现有 articleLoader / feedbackStorage 等保留（0.2.3 功能不受影响）
- [ ] systemPrompt 动态构建（调用 buildSystemPrompt 替代固定字符串）
- [ ] systemPrompt 含新约束（禁止 sessions/ + archived-agents/ + ui-config.json）

### 3.14 memoryEnabled（Task 18）

- [ ] `AgentProfile.memoryEnabled` 字段缺省视为 true
- [ ] default agent 永不注入 MEMORY.md（即使 memoryEnabled=true 也忽略）
- [ ] professional + memoryEnabled=true → systemPrompt 注入 MEMORY.md
- [ ] professional + memoryEnabled=false → systemPrompt 不注入 MEMORY.md
- [ ] PUT `/api/agents/:slug` 可更新 memoryEnabled 字段
- [ ] LLM 配置字段（model / temperature / maxTokens / reasoningEffort / thinkingType / thinkingBudgetTokens）未设置时 fallback 到 config.defaultModel + 系统默认值

### 3.15 archive（Task 19）

- [ ] `archiveAgent` 归档路径 `data/users/<userId>/archived-agents/<slug>-<timestamp>/`
- [ ] 归档包含 AGENT.md + MEMORY.md + sessions/ + memory.log.jsonl
- [ ] 归档后原路径不存在
- [ ] 归档后从 active agent 列表移除
- [ ] 归档失败拒绝删除原目录
- [ ] cpSync 失败时清理归档路径防脏数据
- [ ] DELETE `/api/agents/:slug`（professional）返回 200 + 归档路径
- [ ] DELETE `/api/agents/default` 返回 403
- [ ] 归档文件夹保留在 `archived-agents/` 下，不自动清理

### 3.16 质量验收

- [ ] `npx vitest run` 全量绿（除 ~2 pre-existing auth-api ECONNRESET flaky 已知时序问题）
- [ ] `npx tsc --noEmit` 0 错误（不引入新类型错误）
- [ ] 全部 20 task 在 PLAN-0.3.0.md 中标记 `[x]` 完成

---

## 范围 4：老功能回归核验（不退化）

### 4.1 现有 CLI 命令

- [ ] `/help` 列出所有可用命令（含新增 `/agent` + `/skill`）
- [ ] `/new` 创建新会话（归属当前 agent）
- [ ] `/clear` 清空当前会话消息
- [ ] `/model` 显示当前 model
- [ ] `/exit` 退出（CLI 模式）
- [ ] `/sessions` 列出所有会话
- [ ] `/resume <id>` 切换会话
- [ ] `/label <名称>` 设置会话 label
- [ ] `/session` / `/session.reset` 动态属性命令
- [ ] `/feedback` 命令沿用 0.2.3（list/all/详情/resolve/archive/stats）
- [ ] 未知 `/命令` 不下发给 agent（CommandRegistry 拦截）

### 4.2 现有 WebUI 聊天功能

- [ ] 浏览器打开 `http://localhost:8080` 加载聊天页面正常
- [ ] 发送一条简单消息，agent 正常回复
- [ ] 流式输出正常（文字逐字出现）
- [ ] 工具调用（bash / read / edit）正常执行
- [ ] 工具调用事件在前端正常显示（tool_call 卡片）
- [ ] Session 切换（新建 / 切换 / 刷新保持）
- [ ] 自动重连，支持 `resync_required` 协议
- [ ] 工具结果 max-height 200px + 800 字符截断

### 4.3 现有 /api/* 路由

- [ ] `/api/register` 注册新用户成功
- [ ] `/api/login` 登录成功，返回 token
- [ ] `/api/me` 返回当前用户信息
- [ ] `/api/sessions` 列出会话（按 userId 过滤）
- [ ] `/api/sessions/:id/label` 重命名会话
- [ ] `/api/feedback`（0.2.3）路由沿用
- [ ] 未登录访问受保护接口返回 401
- [ ] 跨用户 session 隔离（用户 A 看不到用户 B 的会话）

### 4.4 现有落地页与 demo

- [ ] landing page（learnPage:true）与 0.2.3 一致（6 section + 知识 nav 链接 + Hero 副标题 + 数据条）
- [ ] /demo demo 页正常加载
- [ ] /learn 列表页 + /learn/:slug 文章页 + /feedback 反馈页沿用 0.2.3
- [ ] landing page nav 链接（首页 / Demo / 中/EN）正常
- [ ] 英文版 `?lang=en` 全页面无中文混入（沿用 0.2.3 修复）

### 4.5 现有基础设施

- [ ] Config 热重载：修改 config/aptbot.json 后下一 turn 生效，不影响进行中 turn
- [ ] Hook 系统：8 hook 点拓扑 + priority 排序 + 异常吞掉 + ctx 链式 mutate（基础设施已测，运行时无插件文件）
- [ ] Skills 系统：两层加载 + L1 索引 + lastUsed 排序 + token 预算截断（基础设施已测，运行时无 skill 文件）
- [ ] Channel 抽象：WebSocket 仍作为 Channel 实现正常工作 + 多客户端同步 + dead-channel 自动 unbind
- [ ] Session 自动摘要命名：新会话首条消息后 turn_end 触发异步摘要生成 + 手动 /label 后固定不再摘要
- [ ] JSONL 历史持久化：重启服务器后刷新页面，旧会话历史正常回放（最近 20 条 message）
- [ ] MixinProvider 多 provider 故障转移（基础设施已测，需多 provider 配置实测）
- [ ] per-sessionKey ring buffer + 全局 LRU 淘汰
- [ ] HttpOnly cookie 安全增强（HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=2592000）
- [ ] turn_busy 响应（同 sessionKey 快速连续发 2 条消息，第二条前端显示"等待中..."）

### 4.6 config 字段不受影响

- [ ] config 其他字段（model / landingPage / learnPage / feedbackEnabled / dataDir / users 等）不受 0.3.0 变更影响
- [ ] defaultConfig 不加 agent 相关字段（agent 系统默认启用，无 opt-in 字段）
- [ ] `feedbackEnabled` 默认 true 不变
- [ ] `learnPage` 默认 false 不变（需 `landingPage: true` 配合）

---

## 核验结果汇总

| 范围 | 项数 | 通过 | 未通过 | 待验证 |
|---|---|---|---|---|
| 1. 本地功能 | 35 | — | — | — |
| 2. VPS 线上 | 7 | — | — | — |
| 3. 新功能 | 110 | — | — | — |
| 4. 旧功能回归 | 41 | — | — | — |
| **总计** | **193** | — | — | — |

---

## 不通过项记录

> 每个未通过项记录：编号 / 问题描述 / 复现步骤 / 严重程度 / 修复方案

| 编号 | 问题描述 | 复现步骤 | 严重程度 | 修复方案 | 状态 |
|---|---|---|---|---|---|
| — | （待核验） | — | — | — | — |

---

## 核验结论

- [ ] **全部通过**（含 VPS 部署后核验）
- [ ] **部分未通过**：修复后重新 UAT
- [ ] **严重回归**：回滚到 0.2.3，重新规划

**核验人：** evan（Web UI）+ assistant（CLI / 测试套件 / VPS 部署代验）
**核验日期：** 待定
**代验依据（实际）：**
- CLI 命令：CommandRegistry 真实路径探针（/agent + /skill 各子命令）
- 测试套件：tests/core/agent/ + tests/core/tool/tools/ + tests/access/agent-api.spec.ts + tests/webui/ + tests/server/ ≈ 123-155 项新增
- Web UI：用户浏览器确认（左侧栏 / 设置浮层 / chip 区 / 记忆 toast）
- VPS：0.3.0 部署到 aptbot.de，HTTPS 200 + 安全头 + agent API 路由可达 + WebSocket 正常

---

## 附录：0.3.0 实施任务对照

| Task | 描述 | 文件 |
|---|---|---|
| Task 1 | AgentProfile 类型与 schema | src/core/agent/agent-profile.ts |
| Task 2 | AgentStorage — AGENT.md 持久化 | src/core/agent/agent-storage.ts |
| Task 3 | default agent 自动创建 + legacy sessions 迁移 | src/core/agent/agent-migration.ts |
| Task 4 | SessionMetadata 扩展 + session 路径迁移 | src/core/memory/types.ts + src/infrastructure/storage/file-storage.ts + src/core/memory/session-repo.ts |
| Task 5 | skill frontmatter template 字段 | src/core/skills/types.ts + src/core/skills/loader.ts |
| Task 6 | read_agent_memory 工具 | src/core/tool/tools/read-agent-memory.ts |
| Task 7 | write_agent_memory 工具 + 审计日志 | src/core/tool/tools/write-agent-memory.ts + src/core/agent/memory-audit-log.ts |
| Task 8 | systemPrompt 自动注入 + KV 缓存 | src/core/agent/system-prompt-builder.ts + src/server.ts |
| Task 9 | Agent HTTP API | src/access/agent-api.ts + src/access/websocket-server.ts |
| Task 10 | UI 配置 API（visibleSkills） | src/core/agent/ui-config.ts + src/access/agent-api.ts |
| Task 11 | Skill HTTP API | src/access/agent-api.ts |
| Task 12 | CLI /agent + /skill 命令 | src/shared/commands/registry.ts + src/cli/index.tsx |
| Task 13 | WebUI 左侧栏树形结构 | src/webui/components/agent-sidebar.ts + agent-node.ts + session-node.ts |
| Task 14 | agent 设置浮层 + 新建 agent 浮层 | src/webui/components/agent-settings-modal.ts + new-agent-modal.ts |
| Task 15 | skill chip 区 + 模板填充 | src/webui/components/skill-chips-bar.ts |
| Task 16 | 记忆写入轻量提示 | src/webui/components/memory-write-toast.ts |
| Task 17 | server.ts 装配 + systemPrompt 约束更新 | src/server.ts + src/core/agent/system-prompt-builder.ts |
| Task 18 | config-types 扩展 + AGENT.md LLM 配置字段 | src/core/agent/agent-profile.ts |
| Task 19 | 删除专业 agent 归档流程 | src/core/agent/agent-storage.ts + src/access/agent-api.ts |
| Task 20 | 封仓收尾 — CHANGELOG / README / ARCHITECTURE / package.json / UAT 核验清单 | package.json + CHANGELOG.md + README.md + README.zh-CN.md + ARCHITECTURE.md + docs/superpowers/plans/0.3.0-uat-checklist.md + PLAN-0.3.0.md |
