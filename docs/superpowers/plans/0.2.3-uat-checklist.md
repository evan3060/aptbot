# aptbot 0.2.3 UAT 核验清单

> **状态：⏳ 待核验**
>
> **前置条件：** 0.2.3 实施任务（Task 1-16）已完成，全量测试套件 0 退化（0.2.2 基线 936/938 + 新增 ~85-95 项，目标总数 ~1030-1050），`npx tsc --noEmit` 0 错误
>
> **核验方式：** 用户逐项核验，通过的勾选 `[x]`，不通过的标记 `❌` 并记录问题，全部修复后重新 UAT
>
> **当前分支：** feat/0.2.3
> **最新 commit：** 3c34e86 (docs: update CHANGELOG, README, ARCHITECTURE for 0.2.3 learn system)
>
> **核验配置：** VPS demo 完整启用 `landingPage:true + learnPage:true + feedbackEnabled:true`
>
> **代验说明：**
> - Web UI 项由用户浏览器确认
> - CLI 项（/feedback）由 assistant 通过 CommandRegistry 真实路径探针代验
> - 测试套件覆盖项在核验时附注测试文件
> - VPS 项待部署后核验

---

## 范围 1：本地功能验证

### 1.1 启动与配置开关

- [ ] `npm run dev` 启动本地服务器无报错
- [ ] config 无 learnPage 时，landing page 保持 v0.2.2 结构（无知识 nav 链接 + 无知识 section + 原 Hero 副标题 + 原数据条）
- [ ] config `learnPage: true` + `landingPage: true` 时，landing page 显示知识 section + nav 知识链接 + Hero 副标题更新（含"学习型项目"）+ 数据条含文章数（19/2/13/6）

### 1.2 /learn 列表页

- [ ] /learn 列表页：显示 19 篇文章卡片（13 Track 1 + 6 Track 2）
- [ ] /learn 列表页：Track 筛选 tab（全部 / Track 1 / Track 2）切换正常，URL hash 记忆（#track1 / #track2）
- [ ] /learn 列表页：chapter 折叠组正常（▼ 入门篇 (2) / ▼ 核心特性深入篇 (8) 等，点击折叠 + localStorage 记忆）
- [ ] /learn 列表页：网格 / 列表视图切换正常（localStorage 记忆，默认网格）

### 1.3 /learn/:slug 文章页

- [ ] /learn/:slug 文章页（published）：显示标题 / meta 行（Track · chapter · 难度 · 阅读时间）/ 摘要 / marked 渲染正文 / 上下篇导航 / 反馈表单
- [ ] /learn/:slug 文章页（planned）：显示 PLANNED 标签 / 大纲列表 / 返回知识体系链接，无反馈表单，无上下篇导航
- [ ] /learn/nonexistent 返回友好 404（含 nav + "文章不存在" + 返回 /learn 链接）
- [ ] /learn/:slug 大写 slug（如 /Learn/xxx）返回 404（正则小写匹配）
- [ ] /learn/:slug 含特殊字符不匹配（如 /learn/01_what 返回 404，/learn/01-what 匹配）

### 1.4 /feedback 反馈页

- [ ] /feedback 页：显示 H1 "留言反馈" + 简介 + 通用反馈表单（category=general 隐藏域，无 articleSlug）
- [ ] /feedback 页：textarea maxlength 2000 required + contact maxlength 120 可选 + 提交按钮 pill + 状态提示区
- [ ] /feedback 页：无反馈数据时空状态显示正常（GET /api/feedback 返回空列表）

### 1.5 文章反馈表单（/learn/:slug 反馈区）

- [ ] 文章反馈表单提交：成功显示"感谢反馈，已记录到待办"（accent 提示）+ 清空表单
- [ ] 文章反馈表单提交：空 message 显示具体错误（400 + field=message）
- [ ] 文章反馈表单提交：限流 429 显示"提交过于频繁，请稍后再试"（连续 11 次 per IP）
- [ ] 文章反馈表单提交：网络错误显示"网络错误，请检查连接"

### 1.6 移动端响应式（390 视口）

- [ ] 移动端列表页：字号缩放 + 卡片网格 1 列 + sticky 筛选栏保持 + chapter 默认折叠，无横向滚动条
- [ ] 移动端文章页：max-width 720px 保持，padding 缩减为 16px，无横向滚动条
- [ ] 移动端反馈页：表单宽度自适应，按钮可点击

---

## 范围 2：VPS 在线验证（如适用）

> **注意：** 0.2.3 待部署到 VPS（aptbot.de + demo.aptbot.de），VPS demo 完整启用 `landingPage:true + learnPage:true + feedbackEnabled:true`。

- [ ] 部署到 VPS 后功能正常（aptbot.de 主站 + demo.aptbot.de demo 站 HTTP 200）
- [ ] 反向代理路由正确：`demo.aptbot.de/learn` 返回列表页 HTML（HTTP 200 + Cache-Control: no-store + X-Content-Type-Options: nosniff）
- [ ] 反向代理路由正确：`demo.aptbot.de/learn/01-what-is-agent` 返回文章页 HTML（HTTP 200）
- [ ] 反向代理路由正确：`demo.aptbot.de/feedback` 返回反馈页 HTML（HTTP 200）
- [ ] learnPage:true 配置在 VPS 生效（落地页含知识 section + nav 知识链接 + Hero 副标题更新 + 数据条含文章数）
- [ ] HTTPS 证书有效，无混合内容警告
- [ ] HTTP 响应头含 `Cache-Control: no-cache, no-store, must-revalidate` + `X-Content-Type-Options: nosniff`

---

## 范围 3：新功能逐项验证

### 3.1 ArticleLoader（Task 1 + Task 2）

- [ ] ArticleLoader 加载 19 篇文章无 warning（启动服务 stderr 无 warning 输出）
- [ ] 19 篇文章全部 status=published，frontmatter 校验通过
- [ ] marked 渲染无 crash（访问每篇文章页返回 200 + 文章 HTML）
- [ ] 热重载：修改文章文件后刷新页面内容更新（无需重启服务）
- [ ] 热重载：mtimeNs 未变返回缓存，变化触发 reload（per-loader mutex 串行化）
- [ ] 边界情况：articles/ 目录不存在返回空 ArticleState + warning，/learn 显示"暂无文章"占位
- [ ] h2/h3 含 id 锚点（slugify），pre 含 data-language 属性

### 3.2 文章内容（Task 13 + Task 14）

- [ ] Track 1 共 13 篇文章（入门篇 2 + 核心特性深入篇 8 + 可靠性+UX 篇 1 + 实战篇 1 + 演进路线篇 1），编号 01-13，order 1-13
- [ ] Track 2 共 6 篇文章（方法论 6），编号 01-06，order 14-19
- [ ] 每篇文章 1500-2500 字，概念性理解为主
- [ ] Track 1 文章 frontmatter track 字段统一为 agent-practice
- [ ] Track 2 文章 frontmatter track 字段统一为 ai-coding-practice，与具体项目无关
- [ ] 逐篇访问 /learn/<slug> 19 篇全部 200 + 渲染正常（Track 1: 01-what-is-agent ~ 13-agent-future-roadmap，Track 2: 01-dev-workflow ~ 06-continuous-improvement）
- [ ] 不含 emoji（CSS 几何符号 / 文字标签替代）

### 3.3 落地页知识 section（Task 7）

- [ ] learnEnabled 时含第 6 section "知识" 锚点
- [ ] nav 含"知识"链接（→ #learn 锚点滚动）
- [ ] Hero 副标题含"学习型项目"+ "19 篇文章带你从 0 理解 agent 原理、实现与演进"
- [ ] Hero 下方含"Demo 是学习内容的实操练习场 —— 看完文章来这里动手验证"小字
- [ ] 数据条含 19 articles / 2 tracks / 13 agent / 6 coding（数字从 ArticleLoader 动态注入，不硬编码）
- [ ] 每 chapter 限显 4 张卡片，超出显示"+N 更多"链接 → /learn
- [ ] planned 卡片 opacity 0.55 + coming soon 标记（0.2.3 默认全部 published，此条留作未来验证）
- [ ] 底部 pill 按钮"查看全部文章 →"跳 /learn
- [ ] 非 learnEnabled 时（learnPage:false）落地页保持 v0.2.2 结构（5 section + 原 Hero 副标题 + 原数据条 + nav 无"知识"链接）

### 3.4 FeedbackStorage（Task 3）

- [ ] append 写入 feedback.jsonl 一行 JSON，自动生成 id（fb-<timestamp>-<rand6>）+ createdAt（ISO 8601）
- [ ] list 默认 status=open + 支持 category 过滤 + 支持 limit+offset 分页 + total 正确
- [ ] moderate 不存在 id 返回 null / 合法 id 重写整个 JSONL 文件 + 返回更新后 entry（status / note / moderatedAt）
- [ ] findById 合法 / 不存在返回 null
- [ ] 并发 append 用 per-file mutex 串行化（无错行）
- [ ] JSONL 破损行容错（skip + warning，不 crash）
- [ ] 文件不存在时 append 自动创建

### 3.5 反馈 API（Task 8）

- [ ] POST /api/feedback 合法请求返回 200 + id
- [ ] POST /api/feedback message 空：400 + field=message
- [ ] POST /api/feedback message > 2000 字符：400
- [ ] POST /api/feedback category=article 但缺 articleSlug：400 + field=articleSlug
- [ ] POST /api/feedback category=article + 不存在 articleSlug：400
- [ ] POST /api/feedback category 非法值：400
- [ ] POST /api/feedback contact > 120 字符：400
- [ ] POST /api/feedback 限流（per IP 10/min + 60/hour）：第 11 次 429 + Retry-After 头
- [ ] POST /api/feedback 限流状态重启重置
- [ ] GET /api/feedback 无 auth token：401
- [ ] GET /api/feedback 错误 auth token：401
- [ ] GET /api/feedback 正确 auth token：200 + items + total
- [ ] GET /api/feedback 正确 token + limit=5 + offset=10：分页正确
- [ ] GET /api/feedback 正确 token + category=bug：过滤正确
- [ ] GET /api/feedback 正确 token + status=open：默认只返回 open
- [ ] POST /api/feedback/:id/moderate 无 auth：401
- [ ] POST /api/feedback/:id/moderate 不存在 id：404
- [ ] POST /api/feedback/:id/moderate 合法：200 + 更新后 entry（status / note / moderatedAt）

### 3.6 /feedback CLI 命令（Task 12）

- [ ] /feedback 注册在 CommandRegistry
- [ ] /feedback 无参数：列出最近 10 条 open 状态（Ink 表格输出 id / category / message 前 40 字符 / createdAt）
- [ ] /feedback all：列出全部状态（含 resolved / archived）
- [ ] /feedback <id>：查看单条详情（含 note / moderatedAt）
- [ ] /feedback resolve <id>：标记为 resolved
- [ ] /feedback resolve <id> <note>：标记为 resolved + 追加 note
- [ ] /feedback archive <id>：归档
- [ ] 无 auth token 时提示设置 APTBOT_AUTH_TOKEN
- [ ] 无 feedbackStorage（feedbackEnabled:false）时提示"反馈功能未启用"

### 3.7 路由与配置开关（Task 9 + Task 10 + Task 11）

- [ ] config `learnPage: false`（或未设置）时 /learn 返回 404（或无路由）
- [ ] config `learnPage: false` 时 /learn/:slug 返回 404
- [ ] config `learnPage: false` 时 /feedback 返回 404
- [ ] config `feedbackEnabled: false` 时 POST /api/feedback 返回 404
- [ ] config `feedbackEnabled: false` 时 GET /api/feedback 返回 404
- [ ] config `feedbackEnabled: false` 时 POST /api/feedback/:id/moderate 返回 404
- [ ] config `landingPage: false`（默认 clone 用户）：/ 是 chat 页，POST /api/feedback 仍接收（feedbackEnabled 默认 true）
- [ ] config `landingPage: true` 但 `learnPage: false`：POST /api/feedback 仍接收
- [ ] 路由分发顺序：/api/feedback 优先于 /api/* > / 与 /demo > /learn > /learn/:slug > /feedback > 404 兜底
- [ ] 所有 HTML 响应含 `Content-Type: text/html; charset=utf-8` + `Cache-Control: no-cache, no-store, must-revalidate` + `X-Content-Type-Options: nosniff`
- [ ] 所有新增 HTML 页面使用 adept tokens CSS 变量 + Inter 字体，视觉与现有落地页一致
- [ ] server.ts 装配：learnEnabled 时 ArticleLoader + FeedbackStorage 实例化 + serveLearnListHtml 等注入
- [ ] server.ts 装配：feedbackEnabled 但非 learnEnabled 时 ArticleLoader 也实例化（供 articleSlug 校验）+ FeedbackStorage 实例化 + serveLearn* 不注入
- [ ] server.ts 装配：都禁用时不实例化 ArticleLoader 与 FeedbackStorage

### 3.8 质量验收

- [ ] `npx vitest run` 全量绿，新增约 85-95 项测试，总数达 ~1030-1050
- [ ] `npx tsc --noEmit` 0 错误（不引入新类型错误）
- [ ] 不含 emoji（用 CSS 几何符号 / 文字标签替代）
- [ ] 文章信任边界内，不引入 DOMPurify（marked 默认转义内联 HTML 中的 <）

---

## 范围 4：老功能回归验证（不退化）

### 4.1 现有 CLI 命令

- [ ] `/help` 列出所有可用命令（含新增 /feedback）
- [ ] `/new` 创建新会话
- [ ] `/clear` 清空当前会话消息
- [ ] `/model` 显示当前 model
- [ ] `/exit` 退出（CLI 模式）
- [ ] `/sessions` 列出所有会话
- [ ] `/resume <id>` 切换会话
- [ ] `/label <名称>` 设置会话 label
- [ ] `/session` / `/session.reset` 动态属性命令
- [ ] 未知 `/命令` 不下发给 agent（CommandRegistry 拦截）

### 4.2 现有 WebUI 聊天功能

- [ ] 浏览器打开 `http://localhost:8080` 加载聊天页面正常
- [ ] 发送一条简单消息，agent 正常回复
- [ ] 流式输出正常（文字逐字出现）
- [ ] 工具调用（bash / read / edit）正常执行
- [ ] 工具调用事件在前端正常显示（tool_call 卡片）
- [ ] Session 切换（新建 / 切换 / 刷新保持）

### 4.3 现有 /api/* 路由

- [ ] `/api/register` 注册新用户成功
- [ ] `/api/login` 登录成功，返回 token
- [ ] `/api/me` 返回当前用户信息
- [ ] 未登录访问受保护接口返回 401
- [ ] 跨用户 session 隔离（用户 A 看不到用户 B 的会话）

### 4.4 现有落地页与 demo

- [ ] landing page（learnPage:false）与 v0.2.2 一致（5 section + 原 Hero 副标题 + 原数据条 + nav 无"知识"链接）
- [ ] /demo demo 页正常加载
- [ ] landing page nav 链接（首页 / Demo / 中/EN）正常

### 4.5 现有基础设施

- [ ] Config 热重载：修改 config/aptbot.json 后下一 turn 生效，不影响进行中 turn
- [ ] Hook 系统：8 hook 点拓扑 + priority 排序 + 异常吞掉 + ctx 链式 mutate（基础设施已测，运行时无插件文件）
- [ ] Skills 系统：两层加载 + L1 索引 + lastUsed 排序 + token 预算截断（基础设施已测，运行时无 skill 文件）
- [ ] Channel 抽象：WebSocket 仍作为 Channel 实现正常工作 + 多客户端同步 + dead-channel 自动 unbind
- [ ] Session 自动摘要命名：新会话首条消息后 turn_end 触发异步摘要生成 + 手动 /label 后固定不再摘要
- [ ] JSONL 历史持久化：重启服务器后刷新页面，旧会话历史正常回放（最近 20 条 message）
- [ ] MixinProvider 多 provider 故障转移（基础设施已测，需多 provider 配置实测）
- [ ] per-sessionKey ring buffer + 全局 LRU 淘汰
- [ ] HttpOnly cookie 安全增强（HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=2592000）
- [ ] turn_busy 响应（同 sessionKey 快速连续发 2 条消息，第二条前端显示"等待中..."）

### 4.6 config 其他字段不受影响

- [ ] config 其他字段（model / landingPage / dataDir / users 等）不受 0.2.3 变更影响
- [ ] defaultConfig 不加 learnPage / feedbackEnabled 字段（undefined → 视为 false / true）

---

## 核验结果汇总

| 范围 | 项数 | 通过 | 未通过 | 待验证 |
|---|---|---|---|---|
| 1. 本地功能 | 22 | 0 | 0 | 22 |
| 2. VPS 线上 | 7 | 0 | 0 | 7（待部署后核验）|
| 3. 新功能 | 75 | 0 | 0 | 75 |
| 4. 旧功能回归 | 36 | 0 | 0 | 36 |
| **总计** | **140** | **0** | **0** | **140** |

---

## 不通过项记录

> 每个未通过项记录：编号 / 问题描述 / 复现步骤 / 严重程度 / 修复方案

| 编号 | 问题描述 | 复现步骤 | 严重程度 | 修复方案 | 状态 |
|---|---|---|---|---|---|
| — | 无不通过项（待核验） | — | — | — | — |

---

## 核验结论

- [ ] **全部通过**（140/140，含 VPS 部署后核验）
- [ ] **部分未通过**：修复后重新 UAT
- [ ] **严重回归**：回滚到 0.2.2，重新规划

**核验人：** evan（Web UI）+ assistant（CLI / 测试套件 / VPS 部署代验）
**核验日期：** 2026-07-01（初验，待实施完成后开始）
**代验依据（计划）：**
- CLI 命令：CommandRegistry 真实路径探针（/feedback 各子命令）
- 测试套件：tests/learn/ + tests/access/ + tests/infrastructure/ + tests/shared/commands/feedback-command.spec.tsx + tests/server/ ≈ 85-95 项新增
- Web UI：用户浏览器确认（列表页 / 文章页 / 反馈页 / 移动端响应式）
- VPS：0.2.3 部署后 aptbot.de + demo.aptbot.de HTTPS 200 + Cache-Control + TLS 证书有效 + /learn 路由可达

---

## 附录：0.2.3 实施任务对照

| Task | 描述 | 文件 |
|---|---|---|
| Task 1 | article-types.ts — 基础类型与 schema | src/learn/article-types.ts |
| Task 2 | article-loader.ts — ArticleLoader 类 | src/learn/article-loader.ts |
| Task 3 | feedback-storage.ts — FeedbackStorage 类 | src/infrastructure/feedback-storage.ts |
| Task 4 | learn-page.ts 列表页 — createLearnListHtml | src/access/learn-page.ts |
| Task 5 | learn-page.ts 文章页 — createLearnArticleHtml | src/access/learn-page.ts |
| Task 6 | learn-page.ts 反馈页 — createFeedbackHtml | src/access/learn-page.ts |
| Task 7 | landing-page.ts 知识 section 扩展 | src/access/landing-page.ts |
| Task 8 | feedback-api.ts — handleFeedbackApi 与限流器 | src/access/feedback-api.ts |
| Task 9 | websocket-server.ts 路由扩展 | src/access/websocket-server.ts |
| Task 10 | config-types.ts 扩展 | src/infrastructure/config-types.ts |
| Task 11 | server.ts 装配 | src/server.ts |
| Task 12 | /feedback CLI 命令 | src/shared/commands/registry.ts + src/cli/index.tsx |
| Task 13 | Track 1 文章（13 篇）| src/learn/articles/01-what-is-agent.md ~ 13-agent-future-roadmap.md |
| Task 14 | Track 2 文章（6 篇）| src/learn/articles/01-dev-workflow.md ~ 06-continuous-improvement.md |
| Task 15 | package.json 依赖与版本 | package.json |
| Task 16 | CHANGELOG / README / ARCHITECTURE 同步 | CHANGELOG.md / README.md / README.zh-CN.md / ARCHITECTURE.md |
| Task 17 | UAT 核验清单 | docs/superpowers/plans/0.2.3-uat-checklist.md |
| Task 18 | 打 tag + 提交 | git tag v0.2.3 |
