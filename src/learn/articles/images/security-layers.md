设计一张现代企业级中文架构图信息图。主题：AI Agent 安全模型——多层纵深防御体系。整体布局：5 圈同心圆布局，从外到内五层防线。最外层 Web 安全层（HttpOnly Secure SameSite cookie、X-Content-Type-Options、WS token 三级优先级）。第二层 API 边界层（API key 仅 env、Bearer token、session ownership 403）。第三层工具执行层（30s 超时 SIGTERM→SIGKILL、TTFB 双时钟）。第四层数据安全层（path-guard、JSONL 自修复、OOM 防护）。最内层 AI 约束层（systemPrompt、LLM 信任边界）。整体风格：Figma UI 风格，蓝白科技风，扁平化矢量图标，细边框卡片，微阴影，浅灰背景，科技感网格纹理。配色：白色背景，蓝色主色调，红色代表安全边界，橙色代表防护层，深蓝文字。画面质量：超高分辨率，专业 PPT 级视觉。size: 1024x1024
