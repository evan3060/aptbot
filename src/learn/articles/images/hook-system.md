设计一张现代企业级中文架构图信息图。主题：AI Agent Hook 系统——8 个扩展点拓扑。整体布局：中心为 Agent Core Loop（ReAct 循环），环绕 8 个 hook 点分布在四层 x before/after。四层：Agent 级（beforeAgent/afterAgent）、Turn 级（beforeTurn/afterTurn）、LLM 级（beforeLLM/afterLLM）、Tool 级（beforeTool/afterTool）。每个 hook 点用小圆形节点虚线连接到核心循环。右侧标注同步执行 + ctx mutate + priority 升序。整体风格：Figma UI 风格，蓝白科技风，扁平化矢量图标，细边框卡片，微阴影，浅灰背景，科技感网格纹理。配色：白色背景，蓝色主色调，橙色代表 hook 点，深蓝文字。中心辐射式环状布局，8 个点均匀分布。画面质量：超高分辨率，专业 PPT 级视觉。size: 1024x1024
